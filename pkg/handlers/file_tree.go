package handlers

// FileTree provides a json representation of the tree
// for demo as of now we will just send the post's dir
// func (rqm *ReqManager) FileTree(c echo.Context) error {
// 	repoPath := filepath.Join(rqm.cfg.BaseDir, rqm.RepoName)

// }
